<h4>Are you sure you want to delete $stylist.getName()'s stylist account?</h4>
<a href="/$stylist.getName()/$stylist.getId()/clients" class="btn btn-success">No</a>
<form action="/$stylist.getName()/$stylist.getId()/delete" method="post">
  <button type="submit" class="btn btn-danger delete">Yes</button>
</form>
